# 2.0.7 — 六页展示层改版

日期：2026-09-20。基于 2.0.6 提交 `1c06462a98f6210938c8af2f41fe9f5b728e8083`。

## 修改范围

- `app.wxss`：新增黑白灰、金色强调的主题变量与六页限定的公共组件样式。保留旧页面兼容样式，未修改登录、设置、详情等页面文件。
- `onboarding`、`dashboard`、`record`、`history`、`report`、`share` 六页 WXML/WXSS：调整字体、间距、表单、数字层级、时间线、分享设置及弹窗视觉。
- `miniprogram/package.json`：版本号改为 2.0.7。
- 附带展示层回归测试和本说明，不增加运行时依赖。

## 严格保留

- 所有原有 JS、WXS、数据表达式、事件、条件、循环、dataset、禁用/加载状态、画布 ID 与媒体懒加载。
- `target-policy.js`、原年龄修改策略、微信与邮箱登录、旧账号绑定、仅自己可见、仅媒体记录、一次编辑、留言与删除确认。
- 所有后端接口、数据库结构及访问权限、性能优化代码。
- 原生导航栏/底栏配置及其 `theme.json` 未改动，仍使用原配色。
- 分享页容器换肤；分享卡仍由原有 `share-card.js` 生成，因此导出图片自身的排版和颜色没有变化。

## 与提供示例的必要差异

示例包含不存在的处理函数/字段、公开切换、千日报告、示例数字、固定一年锁定文案及模拟二维码。直接覆盖会违反“仅展示层”的红线。此次只采用视觉规范，保留真实字段、七天统计和原政策说明，不展示虚构数据或不存在的能力。没有加入模糊滤镜、远程字体或装饰动画。

## 验证

- 190 项测试通过，全量 ESLint、TypeScript 与 `git diff --check` 通过。
- 原 109 个受保护文件中，仅后续明确授权同步的网站 `app/globals.css` 被豁免；其余 108 个运行时/策略/配置/网页逻辑/后端/数据库文件与 2.0.6 基线 SHA-256 一致。
- 六页所有数据表达式和非视觉属性与基线一致，包括多语言钩子、媒体控制、隐私授权及分享状态。
- 在隔离的微信开发者工具工程中，用虚构数据渲染六页实际 WXML/WXSS，检查温和/清醒两种布局、隐私弹窗、中英文长文案、年龄输入与确认区，以及图片/视频/GIF 列表。未访问或修改真实记录。
- 深色主题变量和文本对比度经过静态测试；尚未进行深色模式与不同机型的真机验收。

## 上传状态

先前官方 CLI 返回 `41002: appid missing`。2026-09-20 用户重新登录微信开发者工具后，再次上传成功，CLI 明确返回 `✔ upload`，代码包 432286 字节（422.2 KB），版本 2.0.7，AppID `wxa1ad4ff408b7727d`。未更改 AppID 或运行时代码。

**开发版已上传成功，尚未提交审核、尚未正式发布。** 当前会话无法操作微信公众平台审核后台，需用户在版本管理中选择 2.0.7 并提交审核。上传成功不代表审核通过或上线。

## 后续授权：网站同步发布

- 用户追加要求同步网站，网站只改 `app/globals.css`，所有 TSX、接口及业务逻辑未变。
- 同步灰白/炭黑/金色视觉、明暗主题、温和圆环/清醒数字布局、可见操作按钮；保留原品牌图标。
- 本地浏览器以虚构账户和截获数据检查首页、登录弹窗、温和/清醒模式、记录弹窗、报告及 390px 手机布局。未访问真实账户记录。
- Vercel Next.js 16.2.6 构建成功，构建 16 秒，整次部署约 30 秒。部署来自基线提交加当前工作区未提交的展示层改动。
- 正式部署：`dpl_686mkoFcexmCUsyLzcWhVGciskrc`；已 promote 到 https://app.lifescale.space/ 。
- 候选版本首页与 `/api/miniprogram/service-config` 均 HTTP 200。未改动环境变量或数据库。
- 回退基准：`dpl_HVUuGxHM8mr9zzH7bpNuisD7buoZ`（发布前线上版本）。
